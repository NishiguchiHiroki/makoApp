
<%= render "template/menu_selections" %>
  <div class="menu-container">
    <h2>Reservation</h2>
<div class="menu-contents">
  <% @reservations.each do |reservation| %>
    <form action="" method="post" >
  <div class="menu-package">
  <div class="menu-image">
    <%= link_to "reservation/#{reservation.id}" do%><%= image_tag reservation.image, width:"200px", height:"150px" %><% end %>
      <h4><%= link_to do%><%= reservation.name %><% end %></h4>
   <div class="count-price-container">
     <div class="count-price">
       <div class="price">
         <p>¥<%= reservation.price %><span>(税込)</span></p>
       </div>
       <div class="count">
         <label>
         <select name="">
           <% for count in 0..10 do%>
                 <option value="#{count}"><%= count %></option>
               <% end %>
         </select>
         </label>
      </div>
    </div>
      <input type="submit" value="カートの中に入れる">
   </div>
  </div>
</div>
</form>
<% end %>

</div>
</div>
