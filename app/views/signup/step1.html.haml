.signup-header
  .signup-step1
    = render "signup-header"
    .register-head 
      %h3 アカウント作成
    .register-form-1st
      = form_for @user, url: step2_signup_index_path, method: :post do |f|
        - if session[:provider].blank?
          .form
            .form__title-input
              %span ※必須
              = label :name, 'お名前(全角)'
            .form__input-layout
              %span 姓
              = text_field :last_name, placeholder: '例) メルカリ太郎', value: session[:nickname]
            .form__input-layout-back
              %span 名
              = text_field :first_name, placeholder: '例) メルカリ太郎', value: session[:nickname]

          .form
            .form__title-input
              %span ※必須
              = label :name, 'お名前(カナ)'
            .form__input-layout
              %span 姓
              = text_field :last_name_kana, placeholder: '例) メルカリ太郎', value: session[:nickname]
            .form__input-layout-back
              %span 名
              = text_field :first_name_kana, placeholder: '例) メルカリ太郎', value: session[:nickname]
          .form
            .form__title-input
              %span ※必須
              = label :birthday, '生年月日'
            .form__input-birthday
              .form__input-birthday__content
                %span 年
                = select :birthdate_year, options_for_select(1900..2019), include_blank: '--', class: "birthday"
              .form__input-birthday__content
                %span 月
                = select :birthdate_month, options_for_select(1..12), include_blank: '--', class: "birthday"
              .form__input-birthday__content
                %span 日
                = select :birthdate_day, options_for_select(1..31), include_blank: '--', class: "birthday"
          .form
            .form__title-input
              %span ※必須
              = label :email, 'メールアドレス', class: 'form-label'
            .form__email
              = email_field :email, class: 'form-email', placeholder: 'PC・携帯どちらでも可', value: session[:email]
          .form
            .form__title-input
              %span ※必須
              = label :password, 'パスワード', class: 'form-label'
            .form__password
              %span ※ 英字と数字の両方を含めて設定してください
              = password_field :password,class: "password-input", placeholder: '7文字以上の半角英数字'
            .form__check
              %input{type:"checkbox", class:"check"}
              %span パスワードを表示する
          .form-authentication
            %h3 本人確認欄
            %p 安心・安全にご利用いただくために、お客さまの本人情報の登録にご協力ください。<br>他のお客さまに公開されることはありません。
            %input{type:"checkbox", class:"recaptcha"} 私はロボットではありません
            %p
            「次へ進む」のボタンを押すことにより、
            %a{href: '#'} 利用規約
            に同意したものとみなします
            .next-btn
              %button{type:"submit",class:"btn-red"}
                次へ進む
          .form
            %p.single-text
              %a{href: '#'} 本人情報の登録について  ▷

